# ─────────────────────────────────────────────────────────────
# MATLAB Code Generation Agent — Requirements
# Optimized for 8GB RAM systems with local 1.5–3B Ollama models
# ─────────────────────────────────────────────────────────────

# Core agent UI
rich>=12.0.0

# HTTP client for Ollama health check
requests>=2.28.0

# JSON repair for malformed LLM outputs
json-repair>=0.13.0

# Multi-agent orchestration framework (uses Ollama via OpenAI-compatible API)
ag2[openai]>=0.7.0
